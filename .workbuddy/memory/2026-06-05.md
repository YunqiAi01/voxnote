# 2026-06-05

## Paddle 支付迁移

### 完成的迁移
- 从 Lemon Squeezy 完全迁移到 Paddle Billing
- 在 Paddle 后台创建了 3 个订阅产品及中国地区定价：
  - Monthly: `pri_01ktbsjqma68c342khdnhpyafe` ($4.99 / ¥10 CNY)
  - Yearly: `pri_01ktbswerkczc12t4hx4x2ysy8` ($39.99 / ¥100 CNY)
  - Flash Sale (3个月): `pri_01ktbt103z51v5cp1mg8667dpb` ($9.99 / ¥19.9 CNY)

### 代码变更
1. **`.env.local`**: 替换 LS 变量为 Paddle (PADDLE_API_KEY, PADDLE_WEBHOOK_SECRET, PADDLE_PRICE_*)
2. **`src/app/api/checkout/route.ts`**: 重写为 Paddle API，通过 `POST /transactions` 创建交易，返回 checkout URL 重定向
3. **`src/app/api/webhook/paddle/route.ts`** (新建): 处理 Paddle webhook 事件
   - `transaction.completed`: 支付完成 → 设置 pro
   - `subscription.activated`: 订阅激活 → 设置 pro
   - `subscription.updated`: 续费/暂停/逾期处理
   - `subscription.canceled`: 取消 → 降级免费
   - `subscription.past_due`: 逾期 → 降级免费
4. **`.env.example`**: 更新为 Paddle 变量

### 已完成 ✅
- ~~用户需要在 Paddle 后台配置 Webhook URL~~
- ~~获取 PADDLE_WEBHOOK_SECRET 并填入 .env.local~~
- ~~部署到 Vercel 并添加新的环境变量~~
- Paddle Webhook 已配置 + Vercel 环境变量已添加 + 代码已推送部署

### Bug 修复：登录后支付跳转循环
- **问题**：用户从定价页点击订阅 → checkout API 重定向到 `/login?redirect=/pricing` → 登录后总是跳到 `/` 首页 → 用户再点支付又跳回登录（死循环）
- **根因**：`login/page.tsx` 和 `register/page.tsx` 忽略 URL 中的 `redirect` 参数，硬编码跳转到 `/`
- **修复**：
  1. 两个页面都改为使用 `useSearchParams()` 读取 `redirect` 参数
  2. 登录/注册成功后导航到 `redirect` 指定的页面（默认为 `/`）
  3. 登录页的"注册"链接和注册页的"登录"链接都保留 `redirect` 参数
  4. 使用 `Suspense` 包裹 `useSearchParams`（Next.js 要求）
- **验证**：构建成功，零 TypeScript 错误，已推送到 GitHub → Vercel 自动部署
